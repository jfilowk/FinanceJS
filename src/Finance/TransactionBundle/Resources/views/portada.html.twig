{% extends '::frontend.html.twig' %}

{% block main %}

    <div class="" style="margin-bottom: 20px">
        <select name="userID" id="userID" class="form-control">
            <option value="">All Users</option>
            {% set idArray = [] %}
            {% for transaction in transactions %}
                {% if transaction.user not in idArray %}
                    <option value="{{ transaction.user }}">User {{ transaction.user }}</option>
                    {% set idArray = idArray|merge([transaction.user]) %}
                {% endif %}
            {% endfor %}
        </select>

    </div>

    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">{% if user %} Usuario {{ transactions[0].user }} {% else %} Datos de todos los usuarios {% endif %}</h3>
        </div>
        <div class="panel-body">
            <div class="col-xs-12 col-sm-12 col-md-7 col-lg-7">
                Búsqueda general o por cantidad.
            </div>
            <div class="col-xs-12 col-sm-12 col-md-5 col-lg-5">
                <div class="form-inline">
                    <select name="operation" id="operation" class="form-control">
                        <option value=""> Select one</option>
                        <option value=">"> <</option>
                        <option value="<"> ></option>
                        <option value="="> =</option>
                    </select>
                    <input type="text" name="search" id="search" class="form-control" value="" title=""
                           placeholder="Search"
                           required="required">
                </div>
            </div>

        </div>
        <div id="transactions" class="table-responsive">
            <table class="table table-hover">
                <thead>
                <tr>
                    <th id="th_id">ID</th>
                    <th id="th_amount">Amount</th>
                    <th id="th_date">Date</th>
                    <th id="th_user">User ID</th>
                </tr>
                </thead>
                <tbody>
                {% for transaction in transactions %}
                    <tr>
                        <td>{{ transaction.id }}</td>
                        <td id="amount">{{ transaction.amount }}</td>
                        <td>{{ transaction.transactionDate | date('d-m-Y') }}</td>
                        <td>{{ transaction.user }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
    {% block javascripts %}
        {{ parent() }}
        <script>
            $(document).ready(function () {
                var userId = 1;
                var table_transactions = $('#transactions').find('table');
                $('#userID').change(function () {
                    /*userId = $(this).val();
                    $.post('{{ path('transaction_user_json') }}', {id: userId}, function (data) {
                        console.log(data);
                        table_transactions.empty();
                        $.each(data, function (key, value) {
                            var transaction_date = moment(value['transaction_date']['date'], "YYYY-MM-DD HH:mm Z").format('DD-MM-YYYY');
                            table_transactions.append('\n<tr>\n' +
                            '<td>' + value['id'] + '</td>\n' +
                            '<td id="amount">' + value['amount'] + '</td>\n' +
                            '<td>' + transaction_date + '</td>\n' +
                            '<td>' + value['user'] + '</td>\n' +
                            '</tr>\n')
                        })
                    })*/
                    $.ajax({
                        type: 'POST',
                        url: '/FinanceJS/web/app_dev.php/transaction/ajax/user',
                        data: { id: userId },
                        beforeSend:function(){
                            // this is where we append a loading image
                            table_transactions.find('tbody').remove()
                            table_transactions.add('<tbody>');
                        },
                        success:function(data){
                            $.each(data, function (key, value) {
                                var transaction_date = moment(value['transaction_date']['date'], "YYYY-MM-DD HH:mm Z").format('DD-MM-YYYY');
                                table_transactions.append('\n<tr>\n' +
                                '<td>' + value['id'] + '</td>\n' +
                                '<td id="amount">' + value['amount'] + '</td>\n' +
                                '<td>' + transaction_date + '</td>\n' +
                                '<td>' + value['user'] + '</td>\n' +
                                '</tr>\n')
                            })
                        }
                    })
                });


            });
        </script>
    {% endblock %}

